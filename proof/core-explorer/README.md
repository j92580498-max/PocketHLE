# Core Explorer / SmartBall2 proof

These are PocketHLE framebuffer captures for the supplied `CoreExplorer_eng_demo.ARMV4.CAB` Windows Mobile ARMv4 game. They are emulator captures, not photographs of a physical Windows Mobile device.

- `startup.png` — first non-black frame after startup.
- `gameplay.png` — frame 12 after the two synthetic taps used by `tools/ai-tap-sequence.py`.
- `contact-sheet.png` — all 12 captured frames.

The final run reached `frame_counter=12`, exited cleanly, and emitted no `SetTextAlign` warnings.
