# OddBlob gameplay status

OddBlob now reaches gameplay under the Windows CE runner. The fix restores CAB install registry data before launch, mounts the extracted install tree, and preserves the installed module path so the game can find its resources. Android panic and strategy mode still need a reproducible device crash log before additional compatibility changes are made.

Please attach the Android logcat output captured immediately after selecting Panic or Strategy, together with the game version and screen mode. Do not commit copyrighted CAB or extracted game data.
